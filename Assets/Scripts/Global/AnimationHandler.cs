using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

namespace Global.CrowdSimulation.Animations
{
    public class AnimationHandler : MonoBehaviour
    {
        // private Animator _animator;
        // private FlockAgent _agent;

        // private int _movementHash;

        // private void Start()
        // {
        //     _animator = GetComponent<Animator>();
        //     _agent = GetComponentInParent<NavMeshAgent>();
        //     _movementHash = Animator.StringToHash("ForwardSpeed");
        // }

        // private void Update(){
        //     _animator.SetFloat(_movementHash, _agent.velocity.sqrMagnitude);
        // }
    }
}